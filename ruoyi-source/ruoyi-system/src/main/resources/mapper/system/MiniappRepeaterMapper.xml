<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.MiniappRepeaterMapper">
    <resultMap id="MiniappRepeaterResult" type="MiniappRepeater">
        <id property="repeaterId" column="repeater_id"/>
        <result property="repeaterName" column="repeater_name"/>
        <result property="nameNormalized" column="name_normalized"/>
        <result property="callSign" column="call_sign"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="operationMode" column="operation_mode"/>
        <result property="uplinkFrequencyMhz" column="uplink_frequency_mhz"/>
        <result property="downlinkFrequencyMhz" column="downlink_frequency_mhz"/>
        <result property="status" column="status"/>
        <result property="isPublic" column="is_public"/>
        <result property="lastVerifiedAt" column="last_verified_at"/>
        <result property="radioConfig" column="radio_config"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <sql id="selectRepeaterVo">
        select repeater_id, repeater_name, name_normalized, call_sign, province, city, operation_mode,
               uplink_frequency_mhz, downlink_frequency_mhz, status, is_public,
               last_verified_at, radio_config, remark, create_by, create_time, update_by, update_time
        from miniapp_repeater
    </sql>
    <select id="selectMiniappRepeaterList" parameterType="MiniappRepeater" resultMap="MiniappRepeaterResult">
        <include refid="selectRepeaterVo"/>
        <where>
            <if test="repeaterName != null and repeaterName != ''">and (repeater_name like concat('%', #{repeaterName}, '%') or call_sign like concat('%', #{repeaterName}, '%'))</if>
            <if test="callSign != null and callSign != ''">and call_sign like concat('%', #{callSign}, '%')</if>
            <if test="province != null and province != ''">and province = #{province}</if>
            <if test="city != null and city != ''">and city = #{city}</if>
            <if test="operationMode != null and operationMode != ''">and operation_mode = #{operationMode}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="isPublic != null and isPublic != ''">and is_public = #{isPublic}</if>
        </where>
        order by province, city, repeater_name
    </select>
    <select id="selectMiniappRepeaterById" parameterType="Long" resultMap="MiniappRepeaterResult">
        <include refid="selectRepeaterVo"/> where repeater_id = #{repeaterId}
    </select>
    <insert id="insertMiniappRepeater" parameterType="MiniappRepeater" useGeneratedKeys="true" keyProperty="repeaterId">
        insert into miniapp_repeater(repeater_name, name_normalized, call_sign, province, city, operation_mode, uplink_frequency_mhz, downlink_frequency_mhz, status, is_public, last_verified_at, radio_config, remark, create_by, create_time)
        values(#{repeaterName}, #{nameNormalized}, #{callSign}, #{province}, #{city}, #{operationMode}, #{uplinkFrequencyMhz}, #{downlinkFrequencyMhz}, #{status}, #{isPublic}, #{lastVerifiedAt}, #{radioConfig}, #{remark}, #{createBy}, sysdate())
    </insert>
    <update id="updateMiniappRepeater" parameterType="MiniappRepeater">
        update miniapp_repeater
        <set>
            repeater_name=#{repeaterName}, name_normalized=#{nameNormalized}, call_sign=#{callSign}, province=#{province}, city=#{city},
            operation_mode=#{operationMode}, uplink_frequency_mhz=#{uplinkFrequencyMhz}, downlink_frequency_mhz=#{downlinkFrequencyMhz},
            status=#{status}, is_public=#{isPublic}, last_verified_at=#{lastVerifiedAt}, radio_config=#{radioConfig},
            remark=#{remark}, update_by=#{updateBy}, update_time=sysdate()
        </set>
        where repeater_id=#{repeaterId}
    </update>
    <delete id="deleteMiniappRepeaterByIds">
        delete from miniapp_repeater where repeater_id in
        <foreach collection="array" item="repeaterId" open="(" separator="," close=")">#{repeaterId}</foreach>
    </delete>
</mapper>
