# 2026-09-07

## Nginx 学习文档迭代
- 更新 `server-tutorials/01-Nginx入门与实践.md`：
  - 新增 1.6「域名关联」章节（买域名、ICP 备案提醒、DNSPod A 记录解析、验证、server_name 配置、原理图）。
  - HTTPS 章节顺延为 1.7。
  - 新增 1.9「Nginx 反向代理 Docker 容器应用」：架构 A（宿主机 nginx + `-p` 端口映射，强调 `-p 127.0.0.1:8080:80` 安全写法）与架构 B（docker network + 容器名直连），含 compose 提示和坑速查表。
  - 排查表改为 1.10，练习清单改为 1.11 并补充域名/Docker 练习项。
  - 顺手修复了 1.4 节一个多余的孤立反引号行。

## 此前排查记录（08-19 会话延续）
- 用户腾讯云服务器 nginx 外网无法访问排查：本机 curl/监听/ufw 均正常，疑点集中在安全组与出口 IP 验证；`curl ifconfig.me` 返回空，建议换 ip.sb / cip.cc 等备用站点并检查 DNS。
